;
; PUPPET MANAGED FILE: DO NOT EDIT
; Zone include for DYNAMIC records
; Dynamically generated by Puppet from node Facter data

<%
require '/etc/puppet/modules/bind/templates/helpers.rb'

getAllNodeIPsBySubnet(tgt_network, tgt_netmask).each do |nodename, values|
  values.each do |ip|
     octets = ip.split('.')

-%>
<%= octets[3] %>.<%= octets[2] %>.<%= octets[1] %>.<%= octets[0] %>.in-addr.arpa.	IN PTR	<%= nodename %>.
<% end %>
<% end %>